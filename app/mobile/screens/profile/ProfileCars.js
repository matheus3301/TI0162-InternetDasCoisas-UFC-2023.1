import { StyleSheet, Text, View } from 'react-native';

function ProfileCars() {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>This is the cars screen!</Text>
      </View>
    );
  }

export default ProfileCars;